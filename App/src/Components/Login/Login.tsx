import React, { ReactElement } from "react";

const Login: React.FC = (): ReactElement => {
    return <div style={LoginStyle} className="col-3 ms-3"></div>;
};

const LoginStyle = { zIndex: "1" };

export default Login;
